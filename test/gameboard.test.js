const gameboard = require('../src/factory/gameboard');
const Gameboard = gameboard.Gameboard;

describe('test gameboard', () => {
  test('place ship at specific coordinate', () => {
    expect(0).toBe(1);
  });
});